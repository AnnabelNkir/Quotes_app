import { HighlightVoteDirective } from './highlight-vote.directive';

describe('HighlightVoteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightVoteDirective();
    expect(directive).toBeTruthy();
  });
});
